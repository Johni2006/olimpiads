# Основные зависимости для проекта олимпиадных вопросов

# Парсинг PDF
PyMuPDF>=1.23.0  # fitz - для извлечения текста и изображений из PDF
pypdf2>=3.0.0  # альтернативный парсер PDF

# Обработка изображений
Pillow>=10.0.0  # для работы с изображениями

# База данных (SQLite встроена в Python, но для расширенных возможностей)
# sqlite3 - встроен

# Веб-фреймворк
Flask>=3.0.0  # для API
Flask-CORS>=4.0.0  # для поддержки CORS

# Дополнительные библиотеки
requests>=2.31.0  # для HTTP запросов (уже используется в olympiad_downloader)
beautifulsoup4>=4.12.0  # для парсинга HTML (уже используется)

# Selenium для скачивания (уже используется)
selenium>=4.0.0
webdriver-manager>=4.0.0

# Опционально: OCR для сложных PDF
# pytesseract>=0.3.10
# pdf2image>=1.16.0
